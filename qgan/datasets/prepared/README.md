# Prepared datasets

Each prepared dataset has its own directory:

```text
<dataset_id>/
  data.npz
```

`data.npz` stores NumPy arrays ready for experiments. The dataset recipe is
defined in the experiment configuration.
